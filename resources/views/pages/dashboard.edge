<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Jadwal Tayang | CINEVISION</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-200 font-sans">
  
  @include('partials/nav')

  <main class="container mx-auto px-6 py-8">
    <div class="text-center mb-12">
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-2">NOW SHOWING</h1>
      <p class="text-lg text-gray-400">Pilih film favoritmu dan pesan tiket sekarang!</p>
    </div>
    
    @if(flashMessages.get('success'))
      <div class="p-4 mb-6 text-sm text-green-300 bg-green-900 bg-opacity-50 rounded-lg border border-green-700">{{ flashMessages.get('success') }}</div>
    @endif
    @if(flashMessages.get('error'))
      <div class="p-4 mb-6 text-sm text-red-300 bg-red-900 bg-opacity-50 rounded-lg border border-red-700">{{ flashMessages.get('error') }}</div>
    @endif

    <!-- Filter Form -->
    <div class="bg-gray-800 bg-opacity-50 p-4 rounded-lg shadow-lg mb-8 backdrop-blur-sm border border-gray-700">
      <form method="GET" action="{{ route('dashboard') }}" class="flex flex-col md:flex-row items-center space-y-4 md:space-y-0 md:space-x-4">
        <div class="w-full md:w-auto">
          <label for="date" class="block text-sm font-medium text-gray-400 mb-1">Tanggal</label>
          <input type="date" name="selectedDate" value="{{ selectedDate || '' }}" id="date" class="w-full bg-gray-700 border-gray-600 text-white rounded-md focus:ring-pink-500 focus:border-pink-500">
        </div>
        <div class="w-full md:w-auto">
          <label for="genre" class="block text-sm font-medium text-gray-400 mb-1">Genre</label>
          <select name="selectedGenre" id="genre" class="w-full bg-gray-700 border-gray-600 text-white rounded-md focus:ring-pink-500 focus:border-pink-500">
            <option value="">Semua Genre</option>
            @each(genre in genres)
              <option value="{{ genre.id }}" {{ selectedGenre == genre.id ? 'selected' : '' }}>{{ genre.namaGenre }}</option>
            @endeach
          </select>
        </div>
        <div>
          <button type="submit" class="w-full md:w-auto mt-0 md:mt-6 px-5 py-2 bg-pink-600 text-white rounded-md hover:bg-pink-700">Filter</button>
        </div>
      </form>
    </div>

    <!-- Daftar Jadwal -->
    @each(filmGroup in jadwalsByFilm)
      <div class="mb-12">
        <h2 class="text-3xl font-bold mb-6 text-white">{{ filmGroup.judul }}</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
          @each(jadwal in filmGroup.jadwals)
            <div class="bg-gray-800 rounded-lg shadow-lg overflow-hidden group transform hover:-translate-y-2 transition-transform duration-300">
              <div class="relative">
                <img src="{{ jadwal.film.poster ? asset(jadwal.film.poster) : 'https://placehold.co/400x600/1a1a1a/333333?text=No+Poster' }}" alt="Poster {{ jadwal.film.judul }}" class="w-full h-96 object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-70"></div>
                <div class="absolute bottom-0 left-0 p-4">
                  <h3 class="text-xl font-bold text-white">{{ jadwal.studio.namaStudio }}</h3>
                  <p class="text-3xl font-extrabold text-white">{{ jadwal.jam.substring(0, 5) }}</p>
                </div>
              </div>
              <div class="p-4">
                <p class="text-sm font-semibold text-pink-400 mb-2">Sisa Tiket: {{ jadwal.tiketTersedia }}</p>
                <form action="{{ route('tickets.buy', { id: jadwal.id }) }}" method="POST">
                  {{ csrfField() }}
                  @if (jadwal.tiketTersedia > 0)
                    <button type="submit" class="w-full bg-pink-600 hover:bg-pink-700 text-white font-bold py-2 px-4 rounded-md transition-colors">
                      Beli Tiket
                    </button>
                  @else
                    <button class="w-full bg-gray-600 text-gray-400 font-bold py-2 px-4 rounded-md cursor-not-allowed" disabled>
                      Habis
                    </button>
                  @endif
                </form>
              </div>
            </div>
          @endeach
        </div>
      </div>
    @else
      <div class="bg-gray-800 text-center p-8 rounded-lg shadow-lg">
        <p class="text-gray-400 text-lg">Tidak ada jadwal tayang untuk tanggal atau genre yang dipilih.</p>
      </div>
    @endeach
  </main>
</body>
</html>
  