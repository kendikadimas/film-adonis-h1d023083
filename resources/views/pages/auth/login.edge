<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Login | CINEVISION</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-200 font-sans">
  
  @include('partials/nav')

  <main class="container mx-auto px-6 py-8">
    <div class="flex items-center justify-center min-h-[70vh]">
      <div class="w-full max-w-md p-8 space-y-6 bg-gray-800 rounded-lg shadow-2xl border border-gray-700">
        <h1 class="text-2xl font-bold text-center text-white">
          Login ke Akun Anda
        </h1>

        @if(flashMessages.has('error'))
          <div class="p-4 text-sm text-red-300 bg-red-800 bg-opacity-50 rounded-lg border border-red-700" role="alert">
            {{ flashMessages.get('error') }}
          </div>
        @endif

        <form action="{{ route('auth.login.store') }}" method="POST" class="space-y-6">
          {{ csrfField() }}
          <div>
            <label for="email" class="block mb-2 text-sm font-medium text-gray-300">Email</label>
            <input type="email" name="email" id="email" class="bg-gray-700 border border-gray-600 text-white sm:text-sm rounded-lg focus:ring-pink-500 focus:border-pink-500 block w-full p-2.5 placeholder-gray-400" placeholder="nama@email.com" required>
          </div>
          <div>
            <label for="password" class="block mb-2 text-sm font-medium text-gray-300">Password</label>
            <input type="password" name="password" id="password" class="bg-gray-700 border border-gray-600 text-white sm:text-sm rounded-lg focus:ring-pink-500 focus:border-pink-500 block w-full p-2.5" placeholder="••••••••" required>
          </div>
          <button type="submit" class="w-full px-5 py-3 text-base font-medium text-center text-white bg-pink-600 rounded-lg hover:bg-pink-700 focus:ring-4 focus:ring-pink-800">Login</button>
          <div class="text-sm font-medium text-gray-400 text-center">
            Belum punya akun? <a href="{{ route('auth.register.create') }}" class="text-pink-500 hover:underline">Register di sini</a>
          </div>
        </form>
      </div>
    </div>
  </main>
</body>
</html>
