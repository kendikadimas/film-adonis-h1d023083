    <!DOCTYPE html>
    <html>
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>Tambah Jadwal Baru</title>
      <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans">
      @include('partials/nav')
    
      <main class="container mx-auto px-6 py-8">
        <h1 class="text-2xl font-bold mb-6">Tambah Jadwal Baru</h1>

        <div class="bg-white p-6 rounded-lg shadow-md">
          <form action="{{ route('jadwal.store') }}" method="POST">
            {{ csrfField() }}
            
            @include('pages/admin/jadwal/_form')

            <div class="mt-6 flex justify-end">
              <a href="{{ route('jadwal.index') }}" class="bg-gray-200 hover:bg-gray-300 text-black font-bold py-2 px-4 rounded mr-2">Batal</a>
              <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Simpan Jadwal</button>
            </div>
          </form>
        </div>
      </main>
    </body>
    </html>
    